# NIM Agent — Environment Variables (Optional Development Template)
# In production, keys are entered by the user in the extension UI
# and kept in chrome.storage.session (never committed).

# Optional defaults for local dev testing:
# VITE_DEFAULT_BASE_URL=https://integrate.api.nvidia.com/v1
# VITE_DEFAULT_SEARCH_PROVIDER=brave
